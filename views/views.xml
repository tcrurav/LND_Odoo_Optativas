<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="optativas.list">
      <field name="name">optativas list</field>
      <field name="model">optativas.optativas</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="optativas.action_window">
      <field name="name">optativas window</field>
      <field name="res_model">optativas.optativas</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="optativas.action_server">
      <field name="name">optativas server</field>
      <field name="model_id" ref="model_optativas_optativas"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="optativas" id="optativas.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="optativas.menu_1" parent="optativas.menu_root"/>
    <menuitem name="Menu 2" id="optativas.menu_2" parent="optativas.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="optativas.menu_1_list" parent="optativas.menu_1"
              action="optativas.action_window"/>
    <menuitem name="Server to list" id="optativas" parent="optativas.menu_2"
              action="optativas.action_server"/>
    -->
  </data>
</odoo>